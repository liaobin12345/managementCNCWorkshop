<?xml version="1.0" encoding="utf-8"?>
<configuration>
  <config>
    <!-- 包缓存放到项目目录，避免 ~/.local 权限问题 -->
    <add key="globalPackagesFolder" value=".nuget/packages" />
    <!-- 关闭漏洞审计，避免写入 ~/.local/share 失败 -->
    <add key="audit" value="false" />
  </config>
</configuration>
